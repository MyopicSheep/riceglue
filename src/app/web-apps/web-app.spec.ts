/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {WebApp} from './web-app';

describe('WebApp', () => {
  it('should create an instance', () => {
    expect(new WebApp()).toBeTruthy();
  });
});
