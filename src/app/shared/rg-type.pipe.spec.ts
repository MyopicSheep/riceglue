/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { RgTypePipe } from './rg-type.pipe';

describe('Pipe: RgType', () => {
  it('create an instance', () => {
    let pipe = RgTypePipe;
    expect(pipe).toBeTruthy();
  });
});
